# Rendered parameters file — contains deployment-specific values (tenant IDs, client IDs).
# Copy parameters.example.bicepparam to this file and fill in real values locally.
# Never commit this file.
parameters.bicepparam

# Bicep compiled ARM JSON output (generated by 'az bicep build')
*.json
!*.example.json
